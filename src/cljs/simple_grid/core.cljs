(ns simple-grid.core
  (:require [reagent.core :as r]
            [reagent.dom :as rd]
            [re-frame.core :as rf]
            [taoensso.timbre :as log]))



(defn -main []

  (log/info "Running!"))